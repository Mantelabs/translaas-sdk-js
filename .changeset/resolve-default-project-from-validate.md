---
'@translaas/client': minor
'@translaas/models': minor
---

Resolve default project id from the validate API key response when `defaultProjectId` is omitted, including `TranslaasClient.createAsync` and shared resolution helpers in `@translaas/models`.
